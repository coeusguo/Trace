SBT-raytracer 1.0

// cyl_cyl_refract.ray
// Test the refraction term
// Don't forget to increase the trace depth to >= 2!

camera
{
	position = (5, 5, -5);
	viewdir = (-5, -5, 5);
	updir = (0, 1, 0);
}

directional_light
{
	direction = (-1, -1, -0.2);
	color = (1, 1, 1);
}

directional_light
{
	direction = (0, -5, 5);
	color = (1, 1, 1);
}
scale(2.0,2.0,2.0,
sphere {
	material = { 
		diffuse = (1.0, 1.0, 1.0);
	}
})

